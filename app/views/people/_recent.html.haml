- people.includes(:projects, role_offers: :role).each do |person|
  = render 'people/summary', person: person

- unless people.empty?
  .more= link_to 'More…', people_path(before: people.last, heading_level: @heading_level)
