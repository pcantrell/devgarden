= provide :page_description do
  Information about the “#{role.skill_name}” role in software projects, and a list of people seeking to be #{role.person_name.pluralize} and projects seeking them in Macalester’s Dev Garden.

- heading role.skill_name.titlecase do

  %p= role.responsibilities

  - heading "Projects seeking #{role.person_name.pluralize}" do

    %ul
      - role.role_requests.includes(:project).each do |req|
        %li= link_to req.project.name, req.project

  - heading "People interested in #{role.skill_name}" do

    %ul
      - role.role_offers.includes(:person).each do |offer|
        %li= link_to offer.person.name, offer.person
